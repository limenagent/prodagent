"""
Production AIOps Incident Response Agent — built on the prodagent framework.

Each capability lives in its own module; ``agent.py`` only assembles them.
Run: ``python -m aiops.agent`` (set ``USE_FAKE_LLM=1`` for the offline script).
"""
